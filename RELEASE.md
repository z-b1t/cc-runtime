# cc-runtime v0.1.5

面向 **Cocos Creator 3.x** 预览页 / Web 运行时的 Chrome 调试插件。检测到页面存在 `window.cc` 后，可在侧边栏查看并修改运行中的节点、组件与 ECS 实体，无需回到编辑器。

## 安装

1. 下载本 Release 中的 `cc-runtime-0.1.5.zip` 并解压
2. Chrome 打开 `chrome://extensions`，开启「开发者模式」
3. 「加载已解压的扩展程序」，选择解压后的文件夹（根目录需能看到 `manifest.json`）
4. 打开 Cocos 预览页，点击页面边缘的 **CC** 按钮，或点工具栏图标，打开侧边栏

页面刷新后插件会自动重新注入。若提示注入失败，请确认页面已加载 Cocos Creator（存在 `window.cc`）。

## 功能

### 场景节点

- **节点树**：层级浏览，节点/组件图标对齐编辑器风格；支持按名称搜索、按组件类型过滤
- **节点详情**：查看并修改 Transform、Layer、组件属性；Widget 等控件尽量复刻编辑器样式
- **活动状态**：树中可切换节点 `active`；未激活节点会灰色显示
- **Draw Call**：节点树左侧显示本帧 DC 序号，根节点显示本帧 DC 总数
- **选中刷新**：选中节点后，详情约每 0.5 秒同步一次运行时数据
- **Alt + 点击**：快速展开 / 折叠该节点整棵子树

### 页面交互

- **侦测（拾取）**：在画布上悬停高亮、点击选中节点；支持 2D / 3D；重叠节点可用 Alt + 滚轮切换
- **拖动节点**：开启后可在画布上拖动节点位置（含被遮挡节点）
- **节点闪烁 / 定位**：从树或详情跳回场景中对应节点

### 组件与脚本

- **复制 / 粘贴组件**：复制属性到已有同类组件，或作为新组件粘贴
- **复制 / 粘贴节点变换**：位置、旋转、缩放、Layer
- **跳转源码**：组件脚本、Button / EventHandler 可跳到 Chrome DevTools Sources

### 性能

- **性能面板**：FPS、帧耗时、逻辑 / 物理 / 渲染 / 提交、Draw Call、Instances、三角面、纹理/Buffer 显存、JS 堆（Chromium）
- **系统性能**：对 ECS `System.onUpdate` 采样，查看各系统平均耗时与峰值

### ECS 调试

- **实体树**：World → 类型分组 → 实体；支持搜索与定时刷新
- **实体详情**：查看实体类型、阵营、模板 ID 及组件字段；嵌套数据结构默认折叠，避免一次展开过大

### 界面

- Chrome **侧边栏**，可拖动分割、自由组合窗口
- 节点树 / 节点详情、实体树 / 实体详情会成对切换
- 布局可重置；页面右下角（可贴边拖动）有 **CC** 入口

## 环境要求

- Chrome（Manifest V3）
- 页面为 Cocos Creator 3.x Web / 预览运行时
- ECS 相关面板仅在场景中存在对应 Battle World 时可用

## 已知限制

- 需以「加载已解压的扩展程序」安装，尚未上架 Chrome 应用商店
- 仅调试当前标签页中的运行时，不替代 Creator 编辑器
- ECS 实体详情以只读查看为主
